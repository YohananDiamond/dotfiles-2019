#!/usr/bin/env bash

# TODO: Make something really interesting here:
# Main Menu
# Window Switcher (idk if it's possible)
# File Finder -> Menu Option
# Terminal Command -> Run Directly into Terminal Instance
# TODO: make it usable with rofi and fzf

# Select the engine where the application will run.
_COMMAND='rofi -dmenu'
# _COMMAND='fzf'

compgen -c | ${_COMMAND}
